'use strict';

var React = require('react');

var DummyComponent = React.createClass({
  render: function() {
    return (
      <div>Ceci n'est pas une pipe.</div>
    );
  }
});

module.exports = DummyComponent;
